global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - /etc/prometheus/rules/reliability-alerts.prometheus.yml

scrape_configs:
  - job_name: loklingo-metrics-proxy
    metrics_path: /metrics
    static_configs:
      - targets:
          - loklingo-metrics-proxy:8081
