# Development overlay — wires the backend to the Redis instance on the host (latitude).
# Usage: docker compose -f docker-compose.yml -f docker-compose.dev.yml up

services:
  loklingo-backend:
    environment:
      REDIS_URL: "${REDIS_URL}"
